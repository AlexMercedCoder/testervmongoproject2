// +! HEROKU COMMANDS

// +& check the logs
// ++ heroku logs --tail --app=<appname>

// +& run bash from your projects server
// ++ heroku run bash --app=<appname>

// +! GIT COMMANDS

// +& Add all files in current folder and below
// ++ git add .

// +& commit with message
// ++ git commit -m "message"

// +& add a remote repository
// ++ git remote add <name> <url>

// +& remove a remote
// ++ git remote rm <name>

// +& push a branch to a particular remote
// ++ git push <remoteName> <branchName>